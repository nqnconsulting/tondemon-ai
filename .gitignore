node_modules/
dist/
*.log
.DS_Store

# never commit real secrets or built deploy artifacts
.env
deploy/.env
*.tar
*.tar.gz

# training-data journal: raw records contain REAL client identifiers — never publish
debug-journal/
